import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    dropdown: {
      padding: 12,
      color: '#333',
      fontSize: 16,
      width: 226,
      borderRadius: 4,
    },
})

export {styles}